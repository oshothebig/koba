# koba

プロジェクトの概要は [README.md](README.md) を参照する。

## ツールのインストール方針

イメージに新しくツールを追加する際は、以下の優先順位でインストール方法を選ぶ。

1. 公式に推奨されるインストール方法が `apt` / `mise` 以外の場合は、推奨の方法に従う
2. `mise` でインストール可能なツールは `mise` を優先する
3. 上記に該当しない場合は `apt` を使う
